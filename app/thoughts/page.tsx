import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: '★wik — Thoughts',
  description: 'Fragments from one mind — a small room of thoughts, each one a sheet of paper you can step into.',
}

/** the archive is drawn by the persistent experience in layout.tsx */
export default function ThoughtsPage() {
  return null
}
